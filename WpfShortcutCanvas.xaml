<UserControl x:Class="Click2Key.WpfShortcutCanvas"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:Click2Key"
             Background="{DynamicResource AppBackground}"
             FontFamily="Cairo, Segoe UI">

    <UserControl.Resources>
        <local:FavoriteStarConverter x:Key="FavoriteStarConverter"/>

        <SolidColorBrush x:Key="AppBackground" Color="#09090B"/>
        <SolidColorBrush x:Key="CardBackground" Color="#18181B"/>
        <SolidColorBrush x:Key="PrimaryText" Color="#F4F4F5"/>
        <SolidColorBrush x:Key="SecondaryText" Color="#A1A1AA"/>
        <SolidColorBrush x:Key="BorderColor" Color="#27272A"/>
        <SolidColorBrush x:Key="NavButtonBg" Color="#27272A"/>
        <SolidColorBrush x:Key="NavButtonHover" Color="#3F3F46"/>
        <SolidColorBrush x:Key="ComboBg" Color="#27272A"/>
        <SolidColorBrush x:Key="ComboFg" Color="#F4F4F5"/>

        <Style x:Key="ModernButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource NavButtonBg}"/>
            <Setter Property="Foreground" Value="{DynamicResource PrimaryText}"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="16,9"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderColor}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{DynamicResource NavButtonHover}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="{DynamicResource AppBackground}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ActionButtonStyle" TargetType="Button" BasedOn="{StaticResource ModernButtonStyle}">
            <Setter Property="Background" Value="#1E3A8A"/>
            <Setter Property="BorderBrush" Value="#2563EB"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#2563EB"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="{DynamicResource NavButtonBg}"/>
            <Setter Property="Foreground" Value="{DynamicResource ComboFg}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="ToggleButton"
                                          Background="{TemplateBinding Background}"
                                          BorderBrush="{TemplateBinding BorderBrush}"
                                          BorderThickness="{TemplateBinding BorderThickness}"
                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                          ClickMode="Press">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border x:Name="border"
                                                Background="{TemplateBinding Background}"
                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                BorderThickness="{TemplateBinding BorderThickness}"
                                                CornerRadius="6">
                                            <ContentPresenter HorizontalAlignment="Center"
                                                              VerticalAlignment="Center"
                                                              Margin="{TemplateBinding Padding}"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="border" Property="Background" Value="{DynamicResource NavButtonHover}"/>
                                            </Trigger>
                                            <Trigger Property="IsPressed" Value="True">
                                                <Setter TargetName="border" Property="Background" Value="{DynamicResource AppBackground}"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <ContentPresenter x:Name="ContentSite"
                                              Content="{TemplateBinding SelectionBoxItem}"
                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                              ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                              Margin="{TemplateBinding Padding}"
                                              VerticalAlignment="Center"
                                              HorizontalAlignment="Left"/>
                            <Path x:Name="Arrow"
                                  Fill="{DynamicResource ComboFg}"
                                  Data="M0,0 L3,4 L6,0 Z"
                                  HorizontalAlignment="Right"
                                  VerticalAlignment="Center"
                                  Margin="0,0,8,0"/>
                            <Popup x:Name="Popup" Placement="Bottom"
                                   IsOpen="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}}"
                                   AllowsTransparency="True" Focusable="False">
                                <Grid Width="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=ComboBox}}"
                                      Background="{DynamicResource ComboBg}">
                                    <ScrollViewer MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                        <StackPanel IsItemsHost="True"/>
                                    </ScrollViewer>
                                </Grid>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="{DynamicResource SecondaryText}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ComboBoxItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource ComboFg}"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="Border" Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{DynamicResource NavButtonHover}"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{DynamicResource BorderColor}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernScrollThumb" TargetType="Thumb">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Thumb">
                        <Border Background="{DynamicResource NavButtonHover}" CornerRadius="4" Margin="2,0,2,0"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ScrollBar">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Width" Value="12"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollBar">
                        <Grid Background="{TemplateBinding Background}">
                            <Track x:Name="PART_Track" IsDirectionReversed="true">
                                <Track.Thumb>
                                    <Thumb Style="{StaticResource ModernScrollThumb}"/>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <TextBlock x:Name="txtMainTitle"
                           Text="Connection Between You and Keyboard"
                           Foreground="{DynamicResource PrimaryText}"
                           FontSize="32" FontWeight="Light"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,8"/>

                <Rectangle Height="1" Fill="{DynamicResource BorderColor}"
                           HorizontalAlignment="Stretch" Margin="0,0,0,16"/>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <Button x:Name="btnLang" Content="AR/EN"
                            Click="BtnLang_Click" Style="{StaticResource ModernButtonStyle}"
                            Margin="0,0,12,0"/>
                    <Button x:Name="btnTheme" Content="THEME"
                            Click="BtnTheme_Click" Style="{StaticResource ModernButtonStyle}"
                            Margin="0,0,12,0"/>
                    <Button x:Name="btnLogFile" Content="LOG FILE"
                            Click="BtnLogFile_Click" Style="{StaticResource ModernButtonStyle}"
                            Margin="0,0,12,0"/>
                    <Button x:Name="btnSystemTray" Content="SYSTEM TRAY"
                            Click="BtnSystemTray_Click" Style="{StaticResource ModernButtonStyle}"
                            Margin="0,0,12,0"/>
                </StackPanel>
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Top">
                <Button x:Name="btnDevInfo" Content="DEVELOPER INFO"
                        Click="BtnDevInfo_Click" Style="{StaticResource ModernButtonStyle}"
                        Margin="0,0,24,0"/>

                <TextBlock x:Name="txtTimerLabel" Text="Timer:"
                           Foreground="{DynamicResource SecondaryText}"
                           FontSize="16" VerticalAlignment="Center" Margin="0,0,10,0"/>

                <ComboBox Name="cmbTimerDelay" MinWidth="80" Height="36"
                          HorizontalAlignment="Stretch"
                          SelectedIndex="0"
                          Background="{DynamicResource NavButtonBg}"
                          Foreground="{DynamicResource PrimaryText}"
                          FontSize="15"
                          VerticalContentAlignment="Center"
                          HorizontalContentAlignment="Center">
                    <ComboBoxItem Content="0"/>
                    <ComboBoxItem Content="1"/>
                    <ComboBoxItem Content="2"/>
                    <ComboBoxItem Content="3"/>
                    <ComboBoxItem Content="4"/>
                    <ComboBoxItem Content="5"/>
                </ComboBox>

                <Button x:Name="btnToggleFavorites" Content="☆"
                        Style="{StaticResource ModernButtonStyle}"
                        Width="44" Height="36" Margin="10,0,0,0"
                        FontSize="24" Padding="0,-2,0,0"
                        Click="BtnToggleFavorites_Click"
                        ToolTip="Show Favorites Only"/>
            </StackPanel>
        </Grid>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" PanningMode="VerticalOnly">

            <ItemsControl Name="ShortcutList" VerticalAlignment="Top">

                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Columns="5"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="{DynamicResource CardBackground}"
                                BorderBrush="{DynamicResource BorderColor}"
                                BorderThickness="1"
                                CornerRadius="12"
                                Margin="6"
                                Padding="12"
                                VerticalAlignment="Top">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <TextBlock Text="{Binding DisplayTitle}"
                                           Foreground="{DynamicResource PrimaryText}"
                                           FontWeight="SemiBold" FontSize="16"
                                           HorizontalAlignment="Center"
                                           TextWrapping="Wrap"
                                           Grid.Row="0"/>

                                <TextBlock Text="{Binding ExecutionCount, StringFormat='Count: {0}'}"
                                           Foreground="{DynamicResource SecondaryText}"
                                           HorizontalAlignment="Center"
                                           VerticalAlignment="Center"
                                           FontSize="15"
                                           Grid.Row="1"/>

                                <Grid Grid.Row="2">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <Button Content="{Binding ExecuteButtonText}"
                                            Click="ExecuteButton_Click"
                                            Style="{StaticResource ActionButtonStyle}"
                                            Padding="8,6" FontSize="14"
                                            Margin="0,0,8,0"/>

                                    <Button Grid.Column="1" Content="ⓘ"
                                            Width="36" Height="36"
                                            Style="{StaticResource ModernButtonStyle}"
                                            Padding="0"
                                            ToolTip="{Binding DisplayInfo}"
                                            FontSize="16"/>

                                    <Button Grid.Column="2" Width="36" Height="36"
                                            Margin="6,0,0,0"
                                            Style="{StaticResource ModernButtonStyle}"
                                            Padding="0" FontSize="24"
                                            Click="ToggleFavorite_Click"
                                            Tag="{Binding}">
                                        <Button.Content>
                                            <TextBlock Text="{Binding IsFavorite, Converter={StaticResource FavoriteStarConverter}}"
                                                       VerticalAlignment="Center" 
                                                       HorizontalAlignment="Center"
                                                       Margin="0,-2,0,0"/>
                                        </Button.Content>
                                    </Button>
                                </Grid>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>